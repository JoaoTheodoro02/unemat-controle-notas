{
  "name": "backend-express-firebase-mysql",          // Nome do projeto
  "type": "module",                                   // Permite usar 'import/export' no Node.js
  "main": "src/server.js",                            // Arquivo principal de entrada da aplicação
  "scripts": {
    "start": "node src/server.js"                     // Comando para iniciar o servidor: npm start
  },
  "dependencies": {
    "dotenv": "^16.0.3",                              // Carrega variáveis de ambiente do arquivo .env
    "express": "^4.18.2",                             // Framework para criar APIs com Node.js
    "firebase-admin": "^11.10.1",                     // Biblioteca para autenticação e gerenciamento do Firebase
    "mysql2": "^3.5.2"                                // Cliente para conectar ao MySQL com suporte a Promises
  }
}
